using System.Collections.Generic;
using KemiNETClient.Applets;

namespace KemiNETClient
{
    public class Applet
    {
        public void Load()
        {
            var applets = new List<object>();
            
            applets.Add(new 
            {
                name = List.Name,
            });
        }
    }
}